#ifndef __COMPUTE_H__
#define __COMPUTE_H__

class Compute
    {
    public:
        Compute();
        ~Compute();
        
        bool shouldCompute();
        void compute();
    };

#endif //__COMPUTE_H__
